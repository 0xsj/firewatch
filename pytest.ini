[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts =
    --strict-markers
    --cov=src/app
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    -v